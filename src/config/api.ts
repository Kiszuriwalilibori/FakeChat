export const API_CONFIG = {
  BASE_URL: 'https://randomuser.me/api/',
  TIMEOUT: 10000,
  MAX_RESULTS: 10,
} as const;

// You can now import and use these values like:
// import { API_CONFIG } from './config/api';
// const { BASE_URL, TIMEOUT, MAX_RESULTS } = API_CONFIG;

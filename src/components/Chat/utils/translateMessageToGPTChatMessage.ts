// import { GPTRequestBodyMessage, Message } from "types";

// export const translateMessageToGPTChatMessage = (message: Message) => {
//     let role = "";
//     if (message.sender === "host") {
//         role = "user";
//     } else {
//         role = "assistant";
//     }
//     return { role: role, content: message.content } as GPTRequestBodyMessage;
// };

// export default translateMessageToGPTChatMessage;
export default {}